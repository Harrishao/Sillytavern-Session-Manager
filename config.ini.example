[headless]
st_url = http://127.0.0.1:8000  # SillyTavern 页面地址
headless = true                  # 是否使用无头模式（false 会显示浏览器窗口方便调试）
viewport_width = 600             # 浏览器视口宽度

[timing]
refresh_delay = 3         # 页面刷新后等待时间（秒）
chat_switch_delay = 2     # 切换聊天后等待时间（秒）
